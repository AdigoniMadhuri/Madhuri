#include <stdio.h>

int main() {
    int a,b;
    scanf("%d%d",&a,&b);
    if(b==0)
    {
        printf("second number should not be zero\n");
        b=1;
    }
    int r1=a/b;
    printf("\n%d",r1);


    return 0;
}
